name: Daily Price Update

on:
  schedule:
    # 每天 UTC 时间 01:00 运行 (北京时间 09:00)
    - cron: '0 1 * * *'
    # 每天 UTC 时间 07:00 运行 (北京时间 15:00)
    - cron: '0 7 * * *'
  workflow_dispatch:  # 允许手动触发

# 添加写权限
permissions:
  contents: write

jobs:
  update-prices:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 获取完整历史记录
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install requests openai python-dateutil
    
    - name: Run price scraper
      env:
        MANUS_API_KEY: ${{ secrets.MANUS_API_KEY }}
      run: |
        cd $GITHUB_WORKSPACE
        python price_scraper.py
      continue-on-error: true
    
    - name: Run price calculator
      run: |
        cd $GITHUB_WORKSPACE
        python price_calculator.py
      continue-on-error: true
    
    - name: Commit and push changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        # 检查是否有数据文件生成
        echo "📂 检查数据文件..."
        ls -la data/ || echo "⚠️ data 目录不存在"
        find data/ -name "*.json" -type f || echo "⚠️ 没有找到 JSON 文件"
        # 添加所有数据文件（包括新文件和修改的文件）
        git add data/*.json 2>/dev/null || true
        git add data/ 2>/dev/null || true
        # 显示将要提交的文件
        echo "📋 Git 状态:"
        git status
        echo "📋 暂存的文件:"
        git diff --staged --name-only || echo "没有暂存的文件"
        # 提交并推送
        if ! git diff --staged --quiet; then
          echo "✅ 提交更改..."
          git commit -m "Daily price update $(date +'%Y-%m-%d %H:%M UTC')" || exit 0
          echo "📤 推送到 GitHub..."
          git push origin main || echo "⚠️ 推送失败，但继续执行"
          echo "✅ 数据已推送到 GitHub"
        else
          echo "ℹ️ 没有更改需要提交"
        fi
